import { OnlyCharactersValidDirective } from './only-characters-valid.directive';

describe('OnlyCharactersValidDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyCharactersValidDirective();
    expect(directive).toBeTruthy();
  });
});
